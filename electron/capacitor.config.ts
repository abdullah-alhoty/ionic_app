import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.ionicApp',
  appName: 'ionicApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
